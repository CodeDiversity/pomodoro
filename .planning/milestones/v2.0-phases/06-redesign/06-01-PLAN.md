---
phase: 06-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/theme.ts
  - src/App.tsx
  - src/components/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - App uses light mode color scheme with blue accents
    - Theme provides consistent color tokens for all components
    - Left sidebar navigation is visible with Timer, History, Stats, Settings links
    - Sidebar shows active state with blue highlight
    - Layout structure supports split-pane timer view
  artifacts:
    - path: src/components/ui/theme.ts
      provides: Light mode theme with blue accent colors
      exports: [colors, radii, shadows, transitions, spacing, theme]
    - path: src/components/Sidebar.tsx
      provides: Sidebar navigation component
      exports: [Sidebar]
    - path: src/App.tsx
      provides: Main app layout with sidebar
      contains: sidebar layout structure
  key_links:
    - from: src/App.tsx
      to: src/components/Sidebar.tsx
      via: component import and render
    - from: src/components/Sidebar.tsx
      to: src/components/ui/theme.ts
      via: theme imports for styling
---

<objective>
Establish the foundation for the redesign by creating a light mode theme with blue accents and implementing the sidebar navigation layout. This plan sets up the visual foundation that all other redesign work builds upon.

Purpose: Create the base theme system and layout structure that enables the new light-mode aesthetic with sidebar navigation.
Output: Updated theme.ts with light mode colors, new Sidebar component, refactored App.tsx with sidebar layout.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-redesign/06-CONTEXT.md
@/Users/dev/Documents/youtube/pomodoro/src/components/ui/theme.ts
@/Users/dev/Documents/youtube/pomodoro/src/App.tsx

## Current State
- Theme uses red accent (#e74c3c) with dark-ish surface colors
- App uses top tab-based navigation
- Layout is centered single-column

## Target State (from CONTEXT.md)
- Light mode: White/light gray backgrounds
- Blue accent (#0066FF): Replace red throughout
- Sidebar navigation on left with icons + labels
- Split-pane layout capability
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update theme.ts for light mode with blue accents</name>
  <files>src/components/ui/theme.ts</files>
  <action>
Update the theme.ts file to implement the new light mode color scheme with blue accents:

1. Change primary color from '#e74c3c' (red) to '#0066FF' (blue)
2. Set primaryHover to '#0052CC' (darker blue)
3. Update background to '#FFFFFF' (white)
4. Update surface to '#F5F5F5' (light gray for cards/elevated surfaces)
5. Update border to '#E0E0E0' (subtle gray borders)
6. Update text to '#1A1A1A' (near-black for primary text)
7. Update textMuted to '#666666' (gray for secondary text)
8. Keep success as '#27ae60' (green for checkmarks)
9. Update error to '#DC2626' (red for errors, less prominent now)
10. Add new color tokens:
    - sidebarBg: '#FFFFFF'
    - sidebarBorder: '#E8E8E8'
    - sidebarActive: '#F0F7FF'
    - sidebarActiveText: '#0066FF'

The radii, shadows, transitions, and spacing can remain as-is, though consider adding xl: '32px' to radii for the circular timer button.
  </action>
  <verify>grep -q "primary: '#0066FF'" src/components/ui/theme.ts && grep -q "sidebarBg" src/components/ui/theme.ts</verify>
  <done>Theme exports light mode colors with blue primary and sidebar-specific tokens</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar navigation component</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
Create a new Sidebar component at src/components/Sidebar.tsx:

1. Import styled-components and theme colors
2. Define props interface:
   - activeView: 'timer' | 'history' | 'stats' | 'settings'
   - onViewChange: (view: string) => void

3. Create styled components:
   - SidebarContainer: Fixed width (240px), full height, white background, right border
   - LogoSection: Top section with app name "FocusFlow" or "FocusTime"
   - NavList: Vertical flex container for nav items
   - NavItem: Button-styled nav item with:
     - Icon (use inline SVG for Lucide-style icons: Timer, History, BarChart3, Settings)
     - Label text
     - Active state: light blue background (#F0F7FF), blue text
     - Hover state: light gray background
     - Padding: 12px 16px
     - Gap between icon and text: 12px
     - Border radius: 8px
     - Margin between items: 4px

4. Icons (inline SVG, 20x20, stroke-width 2):
   - Timer: circle with clock hands
   - History: clock with circular arrow
   - Stats: bar chart
   - Settings: gear

5. Render navigation items in order: Timer, History, Statistics, Settings

6. Export Sidebar as default
  </action>
  <verify>test -f src/components/Sidebar.tsx && grep -q "activeView" src/components/Sidebar.tsx</verify>
  <done>Sidebar.tsx exists with styled navigation and icon support</done>
</task>

<task type="auto">
  <name>Task 3: Refactor App.tsx with sidebar layout</name>
  <files>src/App.tsx</files>
  <action>
Refactor App.tsx to use the new sidebar layout:

1. Remove the TabBar and Tab styled components (lines 27-56)
2. Import the new Sidebar component
3. Wrap the app content in a layout structure:
   - Outer container: flex row, full viewport height
   - Sidebar: fixed 240px width
   - Main content: flex-1, scrollable if needed, light gray background (#F8F9FA)

4. Move the HelpPanel and Settings buttons from absolute top-right to:
   - Either keep in main content area top-right
   - Or add to sidebar header area (Claude's discretion based on space)

5. Keep all existing state management and hooks intact
6. Keep all view mode switching logic (timer/history/stats)
7. Pass activeView and onViewChange to Sidebar
8. Wrap the view content (timer/history/stats) in a content area with proper padding

9. Ensure the layout works responsively:
   - Main content should have padding: 24px
   - Content area should have min-height and proper overflow handling

10. Remove the inline styles from the root div and use styled-components for the layout structure
  </action>
  <verify>grep -q "Sidebar" src/App.tsx && grep -q "flex-direction: row" src/App.tsx || grep -q "flex" src/App.tsx</verify>
  <done>App.tsx uses sidebar layout with Sidebar component integrated</done>
</task>

</tasks>

<verification>
- Theme provides light mode blue accent colors
- Sidebar renders with all 4 navigation items
- Active view is highlighted in sidebar
- Layout has sidebar on left, content on right
- All existing functionality preserved
</verification>

<success_criteria>
- theme.ts exports light mode colors with #0066FF primary
- Sidebar.tsx exists and exports Sidebar component
- App.tsx renders sidebar layout
- Navigation between views works via sidebar
- Visual appearance matches light mode mockup direction
</success_criteria>

<output>
After completion, create `.planning/phases/06-redesign/06-01-SUMMARY.md`
</output>
