---
phase: 06-redesign
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/components/Settings.tsx
  - src/components/HelpPanel.tsx
  - src/components/SessionSummary.tsx
  - src/App.tsx
  - src/index.css
autonomous: false

must_haves:
  truths:
    - Settings panel matches light mode design
    - Help panel uses new theme colors
    - Session summary modal redesigned
    - App has consistent light mode background
    - All modals and overlays use new theme
    - Typography is consistent throughout
  artifacts:
    - path: src/components/Settings.tsx
      provides: Redesigned settings panel
      exports: [Settings]
    - path: src/components/HelpPanel.tsx
      provides: Redesigned help panel
      exports: [HelpPanel]
    - path: src/components/SessionSummary.tsx
      provides: Redesigned session summary
      exports: [SessionSummary]
    - path: src/index.css
      provides: Global styles for light mode
  key_links:
    - from: src/App.tsx
      to: src/components/Settings.tsx
      via: component render
    - from: src/App.tsx
      to: src/components/HelpPanel.tsx
      via: component render
---

<objective>
Complete the redesign by updating Settings, HelpPanel, SessionSummary, and global styles. Ensure all components use the new light mode theme consistently.

Purpose: Polish the redesign by updating all remaining components and ensuring visual consistency across the entire app.
Output: Redesigned Settings, HelpPanel, SessionSummary components with updated global styles.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-redesign/06-CONTEXT.md
@/Users/dev/Documents/youtube/pomodoro/src/components/Settings.tsx
@/Users/dev/Documents/youtube/pomodoro/src/components/HelpPanel.tsx
@/Users/dev/Documents/youtube/pomodoro/src/components/SessionSummary.tsx

## Current State
- Settings: Modal with form controls
- HelpPanel: Popup with keyboard shortcuts
- SessionSummary: Modal showing completed session info
- Global styles: Default browser styles

## Target State
- All components use light mode theme
- Consistent card-based modals
- Blue accent colors throughout
- Clean typography
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Settings component</name>
  <files>src/components/Settings.tsx</files>
  <action>
Redesign Settings component for light mode:

1. Modal overlay:
   - Background: rgba(0, 0, 0, 0.5)
   - Backdrop blur: 4px

2. Modal container:
   - Background: white
   - Border-radius: 16px
   - Box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2)
   - Max-width: 480px
   - Padding: 0 (header and content have their own padding)

3. Modal header:
   - Padding: 24px 24px 16px
   - Border-bottom: 1px solid #E8E8E8
   - Title: "Settings", font-size 1.25rem, font-weight 600
   - Close button: X icon top-right, 32px, hover background #F5F5F5

4. Modal content:
   - Padding: 24px
   - Section titles: font-size 0.85rem, font-weight 600, color #666, uppercase, letter-spacing 1px, margin-bottom 16px

5. Form controls:
   - Labels: font-size 0.9rem, color #333, margin-bottom 8px
   - Inputs: border 1px solid #E0E0E0, border-radius 8px, padding 10px 14px
   - Focus: border-color #0066FF, box-shadow 0 0 0 3px rgba(0, 102, 255, 0.1)
   - Checkboxes: custom styled with blue #0066FF when checked
   - Number inputs: with min/max buttons or simple input

6. Toggle switches (for auto-start):
   - Track: 48px wide, 24px tall, rounded
   - Background off: #E0E0E0, on: #0066FF
   - Thumb: 20px circle, white, shadow
   - Smooth transition

7. Footer buttons:
   - Cancel: transparent background, border 1px solid #E0E0E0, color #666
   - Save: background #0066FF, color white, border none
   - Both: padding 10px 20px, border-radius 8px, font-weight 500
   - Hover states for both

8. Keep all existing props and functionality
9. Use theme colors throughout
  </action>
  <verify>grep -q "border-radius: 16px" src/components/Settings.tsx && grep -q "backdrop-blur\|backdrop" src/components/Settings.tsx || grep -q "rgba(0, 0, 0, 0.5)" src/components/Settings.tsx</verify>
  <done>Settings has light mode modal design with blue accents</done>
</task>

<task type="auto">
  <name>Task 2: Redesign HelpPanel component</name>
  <files>src/components/HelpPanel.tsx</files>
  <action>
Redesign HelpPanel for light mode:

1. Trigger button (in App.tsx or component):
   - 40px circle
   - Background: transparent
   - Border: 1px solid #E0E0E0
   - Icon: question mark or help circle (inline SVG)
   - Hover: background #F5F5F5

2. Panel/Modal:
   - Background: white
   - Border-radius: 12px
   - Box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15)
   - Max-width: 400px
   - Padding: 24px

3. Header:
   - Title: "Keyboard Shortcuts", font-size 1.1rem, font-weight 600
   - Close button top-right

4. Shortcut list:
   - Each item: flex row, justify space-between, padding 12px 0
   - Border-bottom: 1px solid #F0F0F0 (except last)
   - Key name: font-weight 500, color #333
   - Key combo: background #F5F5F5, padding 4px 10px, border-radius 4px, font-family monospace, font-size 0.85rem

5. Footer tip:
   - Background: #F0F7FF
   - Border-radius: 8px
   - Padding: 12px 16px
   - Text: "Tip: Press ? anytime to show this help"
   - Font-size: 0.85rem, color #0066FF

6. Keep existing keyboard shortcut functionality
7. Use theme colors
  </action>
  <verify>grep -q "Keyboard Shortcuts" src/components/HelpPanel.tsx && grep -q "border-radius: 12px" src/components/HelpPanel.tsx</verify>
  <done>HelpPanel has light mode design with shortcut list styling</done>
</task>

<task type="auto">
  <name>Task 3: Redesign SessionSummary component</name>
  <files>src/components/SessionSummary.tsx</files>
  <action>
Redesign SessionSummary modal for light mode:

1. Modal overlay:
   - Background: rgba(0, 0, 0, 0.5)
   - Backdrop blur: 4px

2. Modal container:
   - Background: white
   - Border-radius: 16px
   - Box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2)
   - Max-width: 420px
   - Padding: 32px
   - Text-align: center

3. Success icon:
   - 64px circle
   - Background: #E8F5E9 (light green)
   - Checkmark icon: #27AE60, 32px
   - Margin-bottom: 20px

4. Title: "Session Complete!"
   - Font-size: 1.5rem
   - Font-weight: 700
   - Color: #1A1A1A
   - Margin-bottom: 8px

5. Duration display:
   - Text: "You focused for 25:00"
   - Font-size: 1.1rem
   - Color: #666
   - Margin-bottom: 24px

6. Session details card:
   - Background: #F8F9FA
   - Border-radius: 12px
   - Padding: 20px
   - Margin-bottom: 24px
   - Text-align: left

7. Details sections:
   - Notes: show truncated note text, color #333
   - Tags: blue pills as in other components
   - Timestamp: formatted date/time, color #888, font-size 0.85rem

8. Continue button:
   - Full width
   - Background: #0066FF
   - Color: white
   - Border: none
   - Border-radius: 10px
   - Padding: 14px
   - Font-size: 1rem
   - Font-weight: 600
   - Hover: background #0052CC

9. Keep existing props and functionality
10. Use theme colors
  </action>
  <verify>grep -q "Session Complete" src/components/SessionSummary.tsx && grep -q "border-radius: 16px" src/components/SessionSummary.tsx</verify>
  <done>SessionSummary has celebratory light mode design</done>
</task>

<task type="auto">
  <name>Task 4: Update global styles and App.tsx polish</name>
  <files>src/index.css, src/App.tsx</files>
  <action>
Update global styles and App.tsx for final polish:

1. In src/index.css:
   - Set body background to #F8F9FA (light gray)
   - Set text color to #1A1A1A
   - Update font-family to modern stack: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif
   - Add smooth scrolling: scroll-behavior: smooth
   - Remove any dark mode media queries if present
   - Set input, button font-family to inherit

2. In App.tsx:
   - Ensure main content area has background #F8F9FA
   - Update HelpPanel and Settings trigger buttons to match new design
   - Position them in the top-right of the main content area
   - Add proper spacing: top: 24px, right: 24px
   - Ensure all view modes (timer, history, stats) have consistent padding

3. Stats view placeholder:
   - If StatsGrid exists, ensure it has proper container styling
   - Background: white card
   - Border-radius: 12px
   - Padding: 24px
   - Or wrap it in a container with these styles

4. Scrollbar styling (optional but nice):
   - Custom scrollbar for webkit browsers
   - Track: transparent
   - Thumb: #D0D0D0, border-radius 4px
   - Thumb hover: #B0B0B0

5. Focus visible styles:
   - Ensure all interactive elements have visible focus states
   - Use the theme primary color (#0066FF) for focus rings
   - Consistent 2px outline with 2px offset
  </action>
  <verify>grep -q "#F8F9FA" src/index.css && grep -q "font-family" src/index.css</verify>
  <done>Global styles updated for light mode, App.tsx polished</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
Complete redesign implementation including:
- Light mode theme with blue accents
- Sidebar navigation layout
- Circular timer with progress ring
- Split-pane timer view with notes panel
- Card-based history with date grouping
- Redesigned Settings, HelpPanel, SessionSummary
- Consistent global styles
  </what-built>
  <how-to-verify>
1. Run the app: npm run dev
2. Verify sidebar navigation:
   - Timer, History, Statistics, Settings links visible
   - Active state highlighted in blue
   - Clicking switches views
3. Verify timer view:
   - Circular blue progress ring around timer
   - Large time display
   - DEEP WORK badge during focus
   - Play/pause is blue circle, skip/reset are icon buttons
   - Daily goal indicator shows X/8 Sessions
   - Notes panel on right with task input, formatting toolbar, tags
4. Verify history view:
   - Weekly focus hours in header
   - Search bar with filter and calendar
   - Date grouping headers (Today, Yesterday, etc.)
   - Session cards with green checkmarks, blue tags
   - FAB in bottom right
5. Verify settings and help panels open with new design
6. Complete a session to see SessionSummary modal
7. Check all components use light mode (white/light gray backgrounds, blue accents)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Settings opens as light mode modal with blue accents
- Help panel shows keyboard shortcuts in styled list
- Session summary shows celebration UI
- Global background is light gray
- All text is readable with proper contrast
- Focus states are visible
</verification>

<success_criteria>
- Settings.tsx uses light mode modal design
- HelpPanel.tsx uses light mode styling
- SessionSummary.tsx uses celebration design
- index.css sets light mode global styles
- App.tsx has consistent layout and spacing
- All interactive elements have proper focus states
- Visual design is consistent across all components
</success_criteria>

<output>
After completion, create `.planning/phases/06-redesign/06-04-SUMMARY.md`
</output>
