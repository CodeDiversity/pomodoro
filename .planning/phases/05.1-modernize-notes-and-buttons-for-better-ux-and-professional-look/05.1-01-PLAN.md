---
phase: 05.1-modernize-notes-and-buttons-for-better-ux-and-professional-look
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Design tokens are defined and exported from a centralized theme file"
    - "Shared Button component exists with primary, secondary, ghost, and danger variants"
    - "Shared Input and TextArea components exist with consistent focus states"
    - "Shared Chip component exists with consistent styling"
    - "All shared components include focus-visible rings for accessibility"
  artifacts:
    - path: "src/components/ui/theme.ts"
      provides: "Design tokens (colors, radii, shadows, transitions, spacing)"
      min_lines: 50
    - path: "src/components/ui/Button.tsx"
      provides: "Reusable button component with variants"
      exports: ["Button"]
    - path: "src/components/ui/Input.tsx"
      provides: "Reusable input and textarea components"
      exports: ["Input", "TextArea"]
    - path: "src/components/ui/Chip.tsx"
      provides: "Reusable chip/tag component"
      exports: ["Chip", "ChipRemoveButton"]
    - path: "src/components/ui/index.ts"
      provides: "Barrel export for all UI components"
      exports: ["Button", "Input", "TextArea", "Chip", "ChipRemoveButton", "theme"]
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/components/ui/theme.ts"
      via: "import"
      pattern: "import.*theme"
    - from: "src/components/ui/Input.tsx"
      to: "src/components/ui/theme.ts"
      via: "import"
      pattern: "import.*theme"
    - from: "src/components/ui/Chip.tsx"
      to: "src/components/ui/theme.ts"
      via: "import"
      pattern: "import.*theme"
---

<objective>
Create the design system foundation with shared UI components for consistent styling across the application.
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-modernize-notes-and-buttons-for-better-ux-and-professional-look/05.1-RESEARCH.md

**Research summary:**
- Components have inconsistent border-radius (4-12px), minimal shadows, no focus rings
- Recommended approach: Create shared UI components in src/ui/ folder
- Design tokens should include: colors, radii (sm: 4px, md: 8px, lg: 12px), shadows, transitions (150-200ms), spacing scale
- Keep existing red (#e74c3c) accent color
- All interactive elements must have focus-visible rings for accessibility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme.ts with design tokens</name>
  <files>src/components/ui/theme.ts</files>
  <action>
Create src/components/ui/theme.ts with the following design tokens:
- colors: primary (#e74c3c), primaryHover (#c0392b), secondary (#333333), background (#ffffff), surface (#fafafa), border (#e0e0e0), text (#333333), textMuted (#666666), success (#27ae60), error (#e53e3e)
- radii: sm (4px), md (8px), lg (12px), full (9999px)
- shadows: sm (0 1px 2px rgba(0,0,0,0.05)), md (0 2px 8px rgba(0,0,0,0.1)), lg (0 4px 16px rgba(0,0,0,0.15))
- transitions: fast (150ms ease), normal (200ms ease), slow (300ms ease)
- spacing: xs (4px), sm (8px), md (12px), lg (16px), xl (24px)

Export as a typed theme object.
  </action>
  <verify>File exists with complete design tokens</verify>
  <done>theme.ts exports colors, radii, shadows, transitions, and spacing objects</done>
</task>

<task type="auto">
  <name>Task 2: Create Button.tsx with variants</name>
  <files>src/components/ui/Button.tsx</files>
  <action>
Create src/components/ui/Button.tsx with:
- ButtonProps interface with $variant (primary|secondary|ghost|danger), $size (sm|md|lg), $fullWidth
- variantStyles object mapping variants to styled-components css
- sizeStyles object mapping sizes to padding/font-size
- Include transition (200ms), focus-visible ring (2px outline-offset 2px), disabled state (opacity 0.5)
- Use theme colors where applicable
- Export as Button component
  </action>
  <verify>Button component exports and has all variants</verify>
  <done>Button component supports primary, secondary, ghost, danger variants with sm, md, lg sizes</done>
</task>

<task type="auto">
  <name>Task 3: Create Input.tsx with TextArea</name>
  <files>src/components/ui/Input.tsx</files>
  <action>
Create src/components/ui/Input.tsx with:
- TextArea styled component: border, 8px radius, transition (border-color, box-shadow), focus with box-shadow (0 0 0 3px rgba(231,76,60,0.1)), placeholder styling
- Input styled component: same styling pattern as TextArea but for single-line input
- Use theme colors and spacing
- Include focus-visible ring for accessibility
  </action>
  <verify>Both Input and TextArea components export</verify>
  <done>Input and TextArea components exist with consistent focus ring styling</done>
</task>

<task type="auto">
  <name>Task 4: Create Chip.tsx for tags</name>
  <files>src/components/ui/Chip.tsx</files>
  <action>
Create src/components/ui/Chip.tsx with:
- Chip styled component: inline-flex, gradient background (#e74c3c to #c0392b), border-radius 16px, padding, font-weight 500, transition (transform, box-shadow), hover lift effect
- ChipRemoveButton: transparent background, white color, opacity transition, hover opacity 1
- Use theme colors and spacing
  </action>
  <verify>Chip and ChipRemoveButton components export</verify>
  <done>Chip component has gradient background and hover lift effect</done>
</task>

<task type="auto">
  <name>Task 5: Create index.ts barrel export</name>
  <files>src/components/ui/index.ts</files>
  <action>
Create src/components/ui/index.ts that exports:
- theme from './theme'
- Button from './Button'
- Input, TextArea from './Input'
- Chip, ChipRemoveButton from './Chip'
  </action>
  <verify>Barrel export file exists</verify>
  <done>All UI components exported from single index.ts</done>
</task>

</tasks>

<verification>
- [ ] All files created in src/components/ui/
- [ ] Each component uses design tokens from theme.ts
- [ ] All interactive elements have focus-visible styles
- [ ] Components can be imported from index.ts
</verification>

<success_criteria>
Design system foundation complete: theme.ts, Button.tsx, Input.tsx, Chip.tsx, and index.ts all exist with proper exports
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-modernize-notes-and-buttons-for-better-ux-and-professional-look/05.1-01-SUMMARY.md`
</output>
