---
phase: 04-polish-navigation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "Navigation uses proper tab styling (not toggle buttons)"
    - "Active tab has clear visual indicator"
    - "Layout has reduced whitespace"
    - "Components are visually consistent"
  artifacts:
    - path: "src/App.tsx"
      provides: "Updated with tab navigation and tighter layout"
---

<objective>
Polish the UI to make it look more professional with reduced whitespace and proper tab navigation.

Purpose: Improve the visual appearance and usability of the Pomodoro timer.
Output: Updated components with polished UI.
</objective>

<execution_context>
@/Users/michaelrobert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelrobert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-navigation/04-RESEARCH.md
@src/App.tsx
@src/components/TimerDisplay.tsx
@src/components/TimerControls.tsx
@src/components/NotePanel.tsx
@src/components/TagInput.tsx
@src/components/stats/StatCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Upgrade navigation to proper tabs</name>
  <files>src/App.tsx</files>
  <action>
Replace the simple button navigation with a proper tab bar:

1. Replace navigation buttons (lines 226-276) with a TabBar component:
   - Horizontal tab bar with Timer | History | Stats
   - Active tab has underline indicator OR pill background
   - Inactive tabs are muted/gray
   - Position: top-left area, below any header

2. Style the tabs:
   ```typescript
   const TabBar = styled.div`
     display: flex;
     gap: 0.25rem;
     background: #f5f5f5;
     padding: 0.25rem;
     border-radius: 8px;
   `

   const Tab = styled.button<{ $active: boolean }>`
     padding: 0.5rem 1rem;
     border: none;
     border-radius: 6px;
     background: ${props => props.$active ? 'white' : 'transparent'};
     color: ${props => props.$active ? '#333' : '#666'};
     font-weight: ${props => props.$active ? '500' : '400'};
     cursor: pointer;
     box-shadow: ${props => props.$active ? '0 1px 2px rgba(0,0,0,0.1)' : 'none'};
     transition: all 0.2s;
   `
   ```

3. Remove the old button styling and use the new TabBar
4. Keep the viewMode state logic intact
</action>
<verify>
- npm run build passes
- Can switch between tabs
- Active tab is visually distinct
- Timer functionality works
</verify>
<done>
- Navigation is a proper tab bar
- Active indicator is clear
- Smooth transitions
</done>
</task>

<task type="auto">
  <name>Tighten layout spacing</name>
  <files>src/App.tsx</files>
  <action>
Reduce whitespace throughout the app:

1. Container padding: Change from `padding: 1rem` to `padding: 0.5rem`

2. Timer view spacing:
   - Reduce marginTop between TimerDisplay and TimerControls from 2rem to 1rem
   - Reduce NotePanel margin-top from 1rem to 0.75rem
   - Reduce TagInput margin-top from 0.75rem to 0.5rem

3. History/Stats view:
   - Reduce marginTop from 3rem to 1.5rem

4. TimerControls:
   - Reduce gap between buttons from 0.5rem to 0.25rem

5. NotePanel padding: Keep at 1rem but consider reducing to 0.75rem

Keep the centered layout for timer view, just make spacing tighter.
</action>
<verify>
- npm run build passes
- Layout looks more compact but not cramped
- All views render correctly
</verify>
<done>
- Reduced overall whitespace
- Tighter grouping of related elements
- Consistent spacing throughout
</done>
</task>

<task type="auto">
  <name>Refine component styling</name>
  <files>src/components/TimerControls.tsx, src/components/NotePanel.tsx, src/components/TagInput.tsx</files>
<action>
Improve visual consistency across components:

1. TimerControls:
   - Make primary button more prominent (slightly larger, better shadow)
   - Consistent border-radius (8px)

2. NotePanel:
   - Slightly lighter border (#e0e0e0)
   - Consistent border-radius (8px)
   - Ensure focus state matches other components

3. TagInput:
   - Chip styling consistent with design
   - Input border-radius matches NotePanel (8px)
   - Focus state consistent

4. Consider adding subtle transitions to interactive elements

Do NOT change colors dramatically - just refine and consistency.
</action>
<verify>
- npm run build passes
- All interactive elements have consistent styling
- Focus states are visible
</verify>
<done>
- Consistent border-radius across components
- Consistent focus states
- Polished appearance
</done>
</task>

<task type="auto">
  <name>Verify complete polish</name>
  <files>src/App.tsx</files>
  <action>
Final verification of all polish changes:

1. Build: npm run build
2. Test navigation:
   - Can switch between Timer, History, Stats
   - Active tab is clearly indicated
   - Smooth transitions
3. Test timer view:
   - Timer displays correctly
   - Controls work
   - Note panel visible during focus
4. Test history view:
   - Filters work
   - List displays
   - Drawer opens
5. Test stats view:
   - Stats cards display
   - Values show correctly

All views should look polished with reduced whitespace.
</action>
<verify>
- npm run build succeeds
- No console errors
- All views render correctly
- Navigation works
- UI looks professional
</verify>
<done>
- Complete UI polish done
- Phase 4 requirements satisfied
</done>
</task>

</tasks>

<verification>
- Navigation uses proper tab styling
- Active tab has visual indicator (underline or pill)
- Whitespace reduced throughout
- Components visually consistent
- Build passes
- All views functional
</verification>

<success_criteria>
- [ ] NAV-01: Tab-based navigation (Timer | History | Stats)
- [ ] NAV-02: Active tab visually indicated
- [ ] Reduced whitespace throughout
- [ ] Consistent component styling
- [ ] Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-navigation/04-01-SUMMARY.md`
</output>
